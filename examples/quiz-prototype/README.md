# Quiz UI prototype

Run from this directory:

```sh
python3 -m http.server 8080
```

Then open <http://localhost:8080>.

This is an exploratory UI example, not the production implementation. It captures the first playable flow across all 10 questions. The current images, content details, and several screen treatments are review material rather than approved final design.
